<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.hut.soberit.sensors.zephyr.harness"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:name="BioHarnessApplication"
        >
        
        <activity android:name=".BioHarnessSettings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="eu.mobileguild.bluetooth.BluetoothPairingActivity"
                  android:label="@string/paired_devices"
       	          android:theme="@android:style/Theme.Dialog"
				 />

		<receiver android:name="fi.hut.soberit.sensors.harness.BioHarnessBroadcaster$Discover" >
            <intent-filter>
                <action android:name="fi.hut.soberit.sensors.action.DRIVER_DISCOVERY" />
            </intent-filter>
		</receiver>
		
       <service android:name="fi.hut.soberit.sensors.harness.BioHarnessBroadcaster" android:process=":xx">
			<intent-filter>
				<action android:name="fi.hut.soberit.sensors.harness.BioHarnessBroadcaster" />
			</intent-filter>
		</service>

		<receiver android:name=".BioHarnessSessionStart" >
          <intent-filter>
                <action android:name="fi.hut.soberit.sensors.action.SESSION_STARTED" />
            </intent-filter>
		</receiver>			
    </application>
    
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
</manifest>